[build-system]
requires = ["maturin>=1.9.4,<2.0"]
build-backend = "maturin"

[project]
name = "m4againpy"
version = "0.0.1"
description = "PyO3 bindings for AAC/M4A gain adjustment"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.maturin]
features = ["python"]
